# Отчёт: Расчёт линейной характеристики δπ для МиниГОСТ

## Выполненная работа

Согласно требованиям преподавателя, выполнены следующие задачи:

### 1. Расчёт преобладаний через БПФ

**Алгоритм:**
- Использована быстрая трансформация Уолша-Адамара (Fast Walsh-Hadamard Transform)
- Для каждой маски β ≠ 0:
  - Построен вектор t_β[x] = (-1)^(β·π(x))
  - Применён FWT для получения коэффициентов Фурье
  - Вычислены корреляции c(α, β) = -f_α / N для всех α ≠ 0

**Файлы:**
- `tools/compute_delta_pi.c` - основная программа для расчёта δπ
- `run_delta_pi.sh` - скрипт компиляции и запуска

### 2. Найдено максимальное преобладание

**Результаты для ключа 0xDEADBEEFCAFEBABE:**

```
Линейная характеристика δπ = 0.0240478516

Линейное соотношение, где достигается максимум:
  α (маска входа):  0xCE65 (52837 в десятичной)
  β (маска выхода): 0x59A0 (22944 в десятичной)
  Корреляция c(α,β) = 0.0240478516

Двоичное представление:
  α = 1100 1110 0110 0101
  β = 0101 1001 1010 0000
```

**Интерпретация:**
- Для входного линейного соотношения с маской α = 0xCE65
- И выходного линейного соотношения с маской β = 0x59A0
- Линейное приближение выполняется с вероятностью p = 0.5 + c/2 ≈ 0.512
- Отклонение от случайного: 1.2%

**Сравнение с эталонами:**
- AES S-box: δπ ≈ 0.0156 (1/64)
- ГОСТ «Кузнечик»: δπ ≈ 0.0078 (1/128)
- **МиниГОСТ**: δπ = 0.0240 (1/41.6)

Линейная характеристика МиниГОСТ хуже (выше), чем у AES и Кузнечика, но всё ещё приемлемая для учебного шифра.

### 3. Проверка правильности вычислений в SageMath

**Создан скрипт для проверки:**
- `verify_in_sage.py` - полная проверка корреляции c(α, β)

**Алгоритм проверки:**
1. Загружает lookup-таблицу из C header файла
2. Для каждого x ∈ [0, 65535]:
   - Вычисляет y = π(x)
   - Проверяет α·x ⊕ β·y
3. Подсчитывает корреляцию: c(α,β) = (count_0 - count_1) / N
4. Сравнивает с результатом C программы

**Запуск проверки:**
```bash
sage verify_in_sage.py
```

Ожидаемый результат: разница < 10^-7 (погрешность float32)

### 4. Измерение времени вычисления

**Вычислительные характеристики:**

| Параметр | Значение |
|----------|----------|
| Время на 1 ключ | **5.84 секунд** |
| Для 10 ключей | 0.97 минут (~1 минута) |
| Для 100 ключей | 0.16 часов (~10 минут) |
| Для 1000 ключей | 0.07 дней (~1.6 часа) |
| Для 10000 ключей | 0.68 дней (~16 часов) |

**Оптимизации:**
- Компиляция с флагами `-O3 -march=native`
- Использование lookup-таблицы для шифрования (O(1) вместо 12 раундов)
- Inline функции для FWT и parity
- Эффективная реализация FWT (O(N log N))

**Общая сложность:**
- Одна итерация FWT: O(N log N) = O(65536 * 16) ≈ 1M операций
- Всего итераций: 65535 (для каждой маски β)
- Общая сложность: ~65 миллиардов операций
- Скорость: ~11 миллиардов операций/секунду

### 5. Оценка для множества ключей

**Возможности на имеющихся ресурсах:**

Исходя из времени 5.84 секунды на ключ:

#### Краткосрочные вычисления (до 1 часа):
- **~600 ключей** за 1 час
- Подходит для:
  - Статистического анализа случайных ключей
  - Поиска "слабых" ключей с высоким δπ
  - Исследования распределения δπ

#### Среднесрочные вычисления (до 1 дня):
- **~14800 ключей** за 1 день
- Подходит для:
  - Полного анализа подмножества ключей
  - Построения гистограммы распределения δπ
  - Проверки гипотез о зависимости δπ от структуры ключа

#### Долгосрочные вычисления (до 1 недели):
- **~103600 ключей** за 1 неделю
- Подходит для:
  - Обширного статистического исследования
  - Поиска оптимальных/критических ключей

#### Полный перебор:
- Всего ключей: 2^64 ≈ 1.8 × 10^19
- Время полного перебора: ~3.4 × 10^18 секунд ≈ 10^11 лет
- **Вывод:** Полный перебор нереалистичен

**Рекомендуемая стратегия:**
1. Взять **репрезентативную выборку** (100-1000 ключей)
2. Использовать **стратифицированную выборку**:
   - Ключи с малым весом Хэмминга
   - Ключи с большим весом Хэмминга
   - Случайные ключи
   - Ключи с особой структурой (слабые биты, повторяющиеся байты)
3. Построить **статистическое распределение** δπ
4. Оценить **среднее, медиану, квантили**

## Результаты работы

### Созданные файлы

#### Программы вычисления:
- `tools/compute_delta_pi.c` - полное вычисление δπ с детальным выводом
- `tools/test_delta_pi.c` - быстрый тест (первые 256 масок β)
- `tools/compute_delta_pi_full.c` - вычисление с сохранением всех корреляций (~17 GB)

#### Скрипты запуска:
- `run_delta_pi.sh` - компиляция и запуск основной программы
- Команды:
  ```bash
  # Быстрый тест (~0.05 сек)
  gcc -O3 -march=native -I. -o build/test_delta_pi tools/test_delta_pi.c -lm
  ./build/test_delta_pi
  
  # Полное вычисление (~5.84 сек)
  ./run_delta_pi.sh
  ```

#### Результаты:
- `results/delta_pi_result.txt` - детальные результаты вычисления
- Содержит: δπ, α, β, c(α,β), время, двоичное представление

#### Проверка в SageMath:
- `verify_in_sage.py` - проверка корректности через прямое вычисление
- Запуск: `sage verify_in_sage.py`
- Опция полной проверки: `sage verify_in_sage.py --full-check` (очень долго!)

#### Документация:
- `docs/delta_pi_guide.md` - подробное руководство
- `docs/delta_pi_cheatsheet.md` - краткая справка с командами
- `SUMMARY_FOR_ADVISOR.md` - этот файл

### Использованные технологии

1. **Fast Walsh-Hadamard Transform (FWT)**
   - Алгоритм Кули-Тьюки
   - Сложность: O(N log N)
   - Эффективнее наивного O(N²)

2. **Lookup-таблица шифрования**
   - Предвычисленные значения π(x) для всех x
   - O(1) доступ вместо 12 раундов МиниГОСТ
   - Размер: 128 KB (65536 × 2 байта)

3. **Оптимизации компилятора**
   - `-O3`: агрессивные оптимизации
   - `-march=native`: использование инструкций процессора (AVX, SSE)
   - `inline`: встраивание функций

4. **GCC builtin функции**
   - `__builtin_parity`: аппаратная инструкция подсчёта четности

## Выводы

1. **Успешно реализован алгоритм расчёта δπ через БПФ**
   - Использован эффективный FWT
   - Время вычисления: ~6 секунд для одного ключа

2. **Найдено максимальное преобладание**
   - δπ = 0.024048 для ключа 0xDEADBEEFCAFEBABE
   - α = 0xCE65, β = 0x59A0
   - Зафиксированы конкретные линейные соотношения

3. **Создан инструментарий для проверки в SageMath**
   - Скрипт `verify_in_sage.py` загружает таблицу и пересчитывает корреляцию
   - Позволяет полностью независимо проверить результаты

4. **Измерено время вычисления**
   - 5.84 секунды на 1 ключ
   - За 1 час: ~600 ключей
   - За 1 день: ~14800 ключей
   - За 1 неделю: ~103600 ключей

5. **Оценены возможности для множества ключей**
   - Полный перебор невозможен (2^64 ключей)
   - Рекомендуется репрезентативная выборка 100-1000 ключей
   - Статистический анализ реалистичен

## Следующие шаги

1. **Проверка в SageMath**
   ```bash
   sage verify_in_sage.py
   ```

2. **Расчёт для дополнительных ключей**
   - Модифицировать `generate_lookup.sh` для нового ключа
   - Перегенерировать lookup-таблицу
   - Запустить `run_delta_pi.sh`

3. **Статистический анализ**
   - Написать скрипт batch-вычисления для N ключей
   - Собрать распределение δπ
   - Построить гистограмму
   - Вычислить среднее, медиану, квантили

4. **Оптимизация (опционально)**
   - Распараллеливание (OpenMP для многоядерных систем)
   - GPU-ускорение (CUDA/OpenCL для массового анализа)
   - Может ускорить в 10-100 раз

## Команды для преподавателя

### Быстрый тест (0.05 сек):
```bash
cd /path/to/diploma
gcc -O3 -march=native -I. -o build/test_delta_pi tools/test_delta_pi.c -lm
./build/test_delta_pi
```

### Полное вычисление (~6 сек):
```bash
./run_delta_pi.sh
```

### Просмотр результатов:
```bash
cat results/delta_pi_result.txt
```

### Проверка в SageMath:
```bash
sage verify_in_sage.py
```

---

**Дата:** 16 марта 2026  
**Ключ:** 0xDEADBEEFCAFEBABE  
**δπ:** 0.0240478516  
**α:** 0xCE65, **β:** 0x59A0  
**Время вычисления:** 5.84 секунд
