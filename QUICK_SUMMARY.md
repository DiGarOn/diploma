# Краткая сводка для преподавателя

## Выполненная работа

В соответствии с комментарием преподавателя выполнены следующие задачи:

### ✅ 1. Расчёт преобладаний через БПФ

Реализован алгоритм вычисления линейных преобладаний с использованием Fast Walsh-Hadamard Transform (FWT):
- Программа: `tools/compute_delta_pi.c`
- Сложность: O(N² log N) где N = 65536
- Используется lookup-таблица для O(1) шифрования

### ✅ 2. Поиск максимального преобладания

**Для ключа 0xDEADBEEFCAFEBABE найдено:**

```
δπ = 0.0240478516

Линейное соотношение (максимум достигается при):
  α = 0xCE65 (маска входа)  = 1100 1110 0110 0101
  β = 0x59A0 (маска выхода) = 0101 1001 1010 0000
```

**Это означает:** Для линейного соотношения с масками α и β линейное приближение работает с вероятностью p = 0.5 + 0.024/2 ≈ **0.512** (отклонение **1.2%** от случайного).

### ✅ 3. Проверка в SageMath

Создан скрипт `verify_in_sage.py` для независимой проверки:
- Загружает lookup-таблицу из C файла
- Вычисляет корреляцию c(α, β) через прямой подсчёт
- Сравнивает с результатом C программы

**Запуск:** `sage verify_in_sage.py`

**Ожидаемый результат:** Разница < 10^-7 (погрешность float32)

### ✅ 4. Измерение времени

**Результаты на имеющихся ресурсах:**

| Параметр | Значение |
|----------|----------|
| Время на 1 ключ | **5.84 секунд** |
| Процессор | [Ваш процессор с -march=native] |
| Оптимизации | -O3, inline, FWT, lookup-таблица |

### ✅ 5. Оценка для множества ключей

**Возможности вычислений:**

| Время | Количество ключей |
|-------|-------------------|
| 1 минута | ~10 ключей |
| 10 минут | ~100 ключей |
| 1 час | ~600 ключей |
| 1 день | ~14800 ключей |
| 1 неделя | ~103600 ключей |

**Вывод:** Для статистического анализа реалистично вычислить 100-1000 ключей.

**Реализован batch-режим:** `./batch_compute_delta_pi.sh N` для автоматического анализа N случайных ключей с выводом статистики.

---

## Команды для запуска

### Быстрый тест (проверка, ~0.05 сек):
```bash
gcc -O3 -march=native -I. -o build/test_delta_pi tools/test_delta_pi.c -lm
./build/test_delta_pi
```

### Полное вычисление для одного ключа (~6 сек):
```bash
./run_delta_pi.sh
```

**Результаты:**
- Консоль: δπ, α, β, c(α,β), двоичное представление, оценки времени
- Файл: `results/delta_pi_result.txt`
- Скрипт: `verify_in_sage.py` для проверки

### Batch-анализ для 10 ключей (~1 мин):
```bash
./batch_compute_delta_pi.sh 10
```

**Результаты:**
- CSV файл: `results/batch_analysis/delta_pi_results_*.csv`
- Автоматический статистический анализ (среднее, медиана, квартили, гистограмма)

### Проверка независимым вычислением:
```bash
python3 verify_correlation.py
```

**Результат:** ✓ Корреляция подтверждена (разница 3.75e-11)

---

## Интерпретация результатов

**Для МиниГОСТ (ключ 0xDEADBEEFCAFEBABE):**
- δπ = 0.0240 (1/41.6)
- Выше, чем у AES (0.0156) и ГОСТ Кузнечик (0.0078)
- Приемлемо для учебного 16-битного шифра

**Линейное приближение:** Вероятность успеха p ≈ 0.512 вместо идеальных 0.5

---

## Файлы

**Программы:**
- `tools/compute_delta_pi.c` — полное вычисление
- `tools/test_delta_pi.c` — быстрый тест
- `tools/compute_delta_pi_batch.c` — для batch-анализа

**Скрипты:**
- `run_delta_pi.sh` — запуск полного вычисления
- `batch_compute_delta_pi.sh` — batch-анализ
- `verify_in_sage.py` — проверка в SageMath

**Результаты:**
- `results/delta_pi_result.txt` — детальный отчёт
- `results/batch_analysis/` — результаты batch-анализа

**Документация:**
- `SUMMARY_FOR_ADVISOR.md` — подробный отчёт (этот файл)
- `docs/delta_pi_guide.md` — полная документация
- `docs/delta_pi_cheatsheet.md` — краткая справка

---

**Дата:** 16 марта 2026  
**Ключ:** 0xDEADBEEFCAFEBABE  
**δπ:** 0.0240478516  
**α:** 0xCE65, **β:** 0x59A0  
**Время:** 5.84 секунд
